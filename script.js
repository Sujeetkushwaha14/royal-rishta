const menuButton=document.querySelector('.menu-button');const navLinks=document.querySelector('.nav-links');menuButton.addEventListener('click',()=>{const open=navLinks.classList.toggle('open');menuButton.setAttribute('aria-expanded',open);});document.querySelectorAll('.nav-links a').forEach(link=>link.addEventListener('click',()=>navLinks.classList.remove('open')));document.getElementById('year').textContent=new Date().getFullYear();document.getElementById('enquiryForm').addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget;const d=new FormData(form);const text=`नमस्ते, मैं Royal Rishta Marriage Bureau से संपर्क करना चाहता/चाहती हूँ.\n\nनाम: ${d.get('name')}\nमोबाइल: ${d.get('phone')}\nरिश्ता: ${d.get('for')}\nजानकारी: ${d.get('message')||''}`;const files=['biodata','photo'].map(name=>d.get(name)).filter(file=>file&&file.size);if(files.length&&navigator.share&&(!navigator.canShare||navigator.canShare({files}))){try{await navigator.share({title:'Royal Rishta Marriage Bureau',text,files});return}catch(error){if(error.name==='AbortError')return;}}const encoded=encodeURIComponent(text);window.open(`https://wa.me/916375484886?text=${encoded}`,'_blank','noopener');if(files.length)alert('WhatsApp खुल गया है। कृपया वहाँ attach (📎) दबाकर अपनी चुनी हुई फोटो / बायोडाटा फाइल भेजें।');});
